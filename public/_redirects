# Netlify redirects file

# Redirect default Netlify subdomain to primary domain
https://fusionone-platform.netlify.app/* https://fusiononeplatform.com/:splat 301!

# Redirect www to non-www
https://www.fusiononeplatform.com/* https://fusiononeplatform.com/:splat 301!

# SPA fallback
/* /index.html 200